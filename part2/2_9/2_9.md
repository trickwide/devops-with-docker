```
Added environment variables to docker-compose.yml and changed ENV REACT_APP_BACKEND_URL=http://localhost:8080 in frontend Dockerfile to ENV REACT_APP_BACKEND_URL=http://localhost/api/
```

```
Also changed from pre-built images back to building images from scratch
```
